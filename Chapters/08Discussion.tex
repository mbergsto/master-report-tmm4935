\label{ch:discussion}

This chapter discusses the design, behaviour, and evaluation 
of MLguide and considers what the findings say about the 
hypothesis and research questions stated in 
Chapter~\ref{ch:introduction}. The Discussion is organised by 
topic, with sections on each one. It closes by returning to 
the research questions and the design goals, and by 
summarising limitations and directions for future work.

% Liste temaene i stedet for å si "topic"? Fjerne design goals?

% Ta med overview of findings eller ikke?
% Kan oppsummere key benefits og issues og tolke de? 

% \section{Overview of Findings}
% \label{sec:overview-of-findings}

% The evaluation of MLguide combined user testing across two 
% course projects with an example-based evaluation on three 
% constructed problems. In the first project, 13 of 17 groups 
% used MLguide, and the recommended methods overlapped with the 
% methods the groups used at a high or medium level for most of 
% them. Groups raised both benefits, such as method shortlisting 
% and validation of method choices, and issues, such as missing 
% methods and recommendations that were used too late to 
% influence decisions. In the second project, 9 of 19 groups 
% used the system. The reports highlighted overviews of 
% reinforcement learning methods and confirmation of method 
% choices as benefits, and unclear method ranking and an input 
% form unable to express more complex configurations as issues.

% The example-based evaluation showed that the system returned 
% relevant methods for clearly specified problems, but also 
% methods that were less directly tied to the task. The 
% recommendations were shaped both by the ontology filters and 
% by the literature-based retrieval, and the balance between 
% these two knowledge sources had visible effects on what was 
% returned. The same patterns appeared across the user tests and 
% the examples: well-known methods such as Random Forest, Support 
% Vector Machine, and Neural Network were recommended frequently, 
% while more specialised methods relevant to the task were 
% sometimes missing or ranked lower than expected.

% The following sections discuss the main themes that follow from 
% these observations: how literature-based retrieval shapes the 
% recommendations, how the ontology coverage and structure affect 
% the results, the role of filters and application context, the 
% balance between the two knowledge sources, the starter notebook 
% output, and the feedback mechanism. The chapter closes by 
% returning to the research questions and the design goals, and 
% by summarising limitations and directions for future work.

\section{Method Recommendations and the Role of Articles}

The method recommendations in MLguide are driven by the 
semantic similarity between the user's problem description and 
the titles and abstracts of articles in the database, as 
described in Section~\ref{sec:recommendation-process}. The 
articles retrieved are aggregated into method rankings, 
so the methods that appear in articles closely matching the 
problem are ranked highest. 

\begin{comment}
    
Metodeanbefalinger i resultatene viser flere tendenser til at 
"vanlige" metoder blir prioritert.  Altså metoder med høy "article count" som NN, SVM og RF.
Vanlige metoder referer til høy article count, og at de kan brukes på flere tasks.
ALle disse 3 metodene er nevnt i \ref{sec:machine-learning-methods} som brukt til classification og regression i supervised learning. 
Og supervised learning er som nevnt i \ref{sec:ml-in-product-development-and-production} vanligst rapporterte ml area.
Som også stemmer med fordelingen av artikler innenfor areas i \ref{sec:resulting-database}.

I begge user testing prosjektene er Neural Network anbefalt en del men kun rapportert brukt en gang.
Vi ser også at disse metodene dukker opp en del i anbefalinger i example based evaluation.
RF, SVM og NN har gjennomgående høye article count. Og er tilstede en del. Det er ikke nødvendigvis feil, når de rapportert mye brukt.
Men de kan dukke opp litt mye og mulig gjøre det vanskelig for andre nye eller mindre rapporterte og mer spesifike metoder. Ref \parencite{riesener2020} i
\ref{sec:related-work-and-existing-tools}. Dette ser vi gjennom eksemplene i \ref{sec:example-based-evaluation}
når neural netowrk topper RL problemet med høyest article count.
RF er tilstede på anomaly detection problemet, uten task match. Alle 3 er mer naturlig høyt representert på
supervised classification problem. Men de er rangert helt på topp og heller ikke slavisk etter article count. Så det 
tyder på at de ikke dominerer helt. GMM har sneket seg inn høyt der, til tross for bare 33 artikler, og 
det brukes 50 artikler i rangeringen. Og de 3 populære jeg har nevnt har flere tusen. Som indikerer det som er i ranking prosessen med at top 
artikler vektes mest ?

\end{comment}

The recommendations across the evaluation show a tendency to 
favour methods with a high article count, in particular Random 
Forest, Support Vector Machine, and Neural Network. These three 
methods are described in 
Section~\ref{sec:machine-learning-methods} as applicable to 
several tasks, including tasks in supervised learning, which 
Section~\ref{sec:ml-in-product-development-and-production} 
reports as the most frequently used ML area in product 
development and production, with Random Forest, Support Vector 
Machine, and Neural Network among the most frequently used 
methods. This is also reflected in the article database, where 
supervised learning is the dominant area, as shown in 
Section~\ref{sec:resulting-database}. The dominance of these 
methods in the recommendations is therefore not surprising. The 
same methods appear prominently in the example-based 
evaluation: Neural Network is the top recommendation in the 
reinforcement learning example with the highest article count, 
above several specific Reinforcement Learning based methods.
Random Forest is returned in the anomaly detection example 
despite not matching the task. In the user testing, Neural 
Network was recommended to several groups in both projects, 
although it was only used once. 

This effect has been pointed out for literature-based selection 
by \textcite{riesener2020}, who note that determining method 
relevance from the number of articles in the literature can put 
newer or less frequently reported methods at a disadvantage. 
MLguide does not rank methods strictly by article count, but 
methods that are mentioned in many articles are more likely to 
appear among the top retrieved articles that drive the method 
ranking, so a similar effect is present. At the same time, 
methods with few articles are not excluded from the top of 
the list. In the supervised classification example shown in 
Figure~\ref{fig:recommendations-example-1-rec}, Gaussian 
Mixture Model appears relatively high despite having only 33 
articles, against several thousand for Random Forest, Support 
Vector Machine, and Neural Network. The same effect is visible in the 
anomaly detection example shown in 
Figure~\ref{fig:recommendations-example-3-rec}, where Local 
Outlier Factor is ranked second despite having only 48 articles. 
This shows that a method with few articles can still rank high 
if those articles match the problem description closely.

\begin{comment}

En anne case med artiklene er at vi bruker abstracts, kontra full tekst. 
Det er praktisk naturligvis når man skal lagre ca. 17000 artikler, og 
sammenligne de fortløpende med problemstilling. I tillegg er problemstilling mer 
sammenlignbar i størrelse med en abstract enn full tekst. Og kan bruke samme sentence 
embedding modeller. Men som flere kilder peker på i \ref{sec:semantic-retrieval-from-scientific-literature}.
Det nevnes der at abstract er en oppsummering. Noe som kan føre til oppramsing av metoder brukt, 
og så nevne at en var best f.eks.. Om en metode blir nevnt som dårligst til en task merkes ikke av 
retrieval systemet her. Siden metoder bare hentes hvis den nevnes. Lignende problem kan gjelde i 
klassifisering av ml area basert på keywords. Og en abstract kan presentere
at en approach har blitt gjort unsupervised men vi skal teste supervised. Da vil begge ml areas få treff.
\end{comment}

Another aspect of the article-based recommendation is that 
MLguide uses article titles and abstracts as the text input for 
retrieval, not full article content. The motivation for this 
choice is described in 
Section~\ref{sec:article-retrieval-and-knowledge-extraction}: 
abstracts provide a consistent representation of each article's 
core content, while full texts include sections that make them 
less comparable across a corpus, and storing and matching around 
17000 full texts would also be costly. Abstracts are also more 
comparable in length to a problem description, which suits the 
same sentence embedding model. However, several works reviewed in 
Section~\ref{sec:semantic-retrieval-from-scientific-literature} 
point to the limitations of this approach. 
\textcite{park2026chain_of_retrieval} report that relying only 
on abstracts is suboptimal because abstracts offer only sparse, 
high-level summaries of a paper, and 
\textcite{ostendorff2022_aspect_based_embeddings} find that 
embeddings of titles and abstracts capture the task and dataset of a 
paper well but its method poorly, because methodological detail 
is mentioned only marginally in titles and abstracts.

The summary character of abstracts has visible consequences for 
MLguide. An abstract can list several methods used in a study, 
for example mentioning that one was the best while others were 
tested as baselines, but the method retrieval does not 
distinguish between them. All mentioned methods are treated as 
relevant to the problem the article addresses. The same applies 
when a method is used as part of a pipeline rather than as the 
main contribution, such as a dimensionality reduction step 
applied before the main method. Principal Component Analysis, 
described in Section~\ref{sec:machine-learning-methods} as a 
dimensionality reduction method, appears among the 
recommendations in the supervised classification example 
(Section~\ref{sec:example-1}), likely for this reason. The same 
issue applies to the classification of articles into ML areas, 
which is based on keywords in titles and abstracts. An article 
that presents an unsupervised approach while contrasting it 
with a supervised baseline can match both ML areas. As a 
result, methods that are mentioned but not recommended by the 
original authors, that are used as preprocessing steps, or 
that belong to a different ML area than the article's main 
contribution, can still appear in the recommendations.


\begin{comment}

Videre kan jeg si at mine funn matcher på et vis på at "task and dataset" er bedre dekket.
Siden resultatene alle 3 evaluation based examples (ref) viser at listen over top-articles 
(relevant articles i UI) viser for det meste artikler som er god match til problemstillingen. I tillegg nevner P2 user test
at MLguide var relevant for å finne artikler. 
Og problemstillingen inneholder jo gjerne forsøk på å beskrive task og dataset, mer enn metode, siden metode er det man
leter etter svar på. 

\end{comment}


The findings in this thesis are in line with the observation 
that task and dataset are captured better than method details in abstracts. 
Across the three example-based evaluations in 
Section~\ref{sec:results-example-based-evaluation}, the 
top-ranked articles for the recommended methods are generally 
relevant to the problem at hand. In the user testing, three 
groups in Project 2 also reported that MLguide was useful for 
finding or confirming relevant articles 
(Section~\ref{sec:project-2}). This reflects that a user's 
problem description tends to describe the task and the data 
more than the method, since the method is what the user is 
looking for. The retrieval therefore appears to work well on 
the topical side of the problem, while methodological match 
is harder to assess from the matching alone.


\begin{comment}

Neste avsnitt kan ta opp en utfordring med å basere seg på uthenting av metodenavn, 
er at man må håndtere caser som neural network er tilstede i ordet recurrent neural network f.eks. 
Slike spesifike caser må håndteres spesifikt i dette systemet. Og i nåværende recommendation prosess
er det tatt hensyn til med neural network som beskrevet i sec:article-retrieval-knowledge-extraction
men dette er ikke tatt hensyn til for alle relevante caser tyder det på. Når SVM topper listen for
anbefalte metoder i anomaly detection eksempelet, og første artikkel nevnt i top articles nevner spesifikt
"One-Class". Og one-class svm er som nevnt i \ref{sec:machine-learning-methods} en anomaly detection metode.
One class svm er litt lenger ned på anbefalingene med kun 34 artikler. Som tyder på at denne får bidrag fra
noen relevante artikler, men svm får det fra de samme som oc-svm men havner høyere grunnet den generelt høye
antall artikler den har, som diskutert tidligere i seksjonen. 

\end{comment}

A further challenge in extracting method names from text is 
handling cases where one method name is contained in another, 
such as Neural Network within Recurrent Neural Network. Each 
such case has to be handled explicitly. The article retrieval 
pipeline described in 
Section~\ref{sec:article-retrieval-and-knowledge-extraction} 
handles this for Neural Network, but the results suggest that 
not all relevant cases are covered. In the anomaly detection 
example, Support Vector Machine is at the top of the 
recommendations, while One-Class SVM appears further down. 
One-Class SVM is described in 
Section~\ref{sec:machine-learning-methods} as an anomaly 
detection method, and the top-ranked articles for Support 
Vector Machine in this example include a paper that explicitly 
concerns a one-class model. This indicates that articles about 
One-Class SVM contribute to the article count of both methods, 
but Support Vector Machine benefits from its much larger 
overall article count and ranks higher as a result.

A related point is that literature-based recommendations 
reflect the methods that appear in published research, which 
may differ from what is commonly used in practice or taught in 
ML courses. In the user testing for the anomaly detection problem in 
Project 1, several groups reported that neural network methods were 
over-represented in the recommendations, and that Gaussian 
Mixture Model and One-Class SVM were missing 
(Section~\ref{sec:project-1}). These are classical methods for 
anomaly detection specifically, while neural network methods 
have a broader range of applications, as described in 
Section~\ref{sec:neural-networks-and-deep-learning}. This 
helps explain why neural network methods have higher article 
counts in the database overall. 
Another possible factor is that research literature tends to use more 
advanced models than those that are typical starting points 
for users new to machine learning, so recommendations grounded 
in research can differ from what students or beginners would 
expect based on their course material. This
The supervised classification 
example in Section~\ref{sec:example-1} shows a different 
balance, where classical methods such as Linear Regression 
and K-Nearest Neighbors also have high article counts. 
Classical methods are therefore not under-represented as a 
whole, but they can be under-represented within specific tasks 
or areas. 

In the supervised classification example 
(Section~\ref{sec:example-1}), Gaussian Mixture Model is 
ranked second despite being flagged as not suited for the 
classification task. A likely reason is that the wording of 
the problem description, which includes terms such as 
"faulty", resembles how an unsupervised anomaly detection 
problem might be described. The literature-based score for 
Gaussian Mixture Model is therefore high enough to overcome 
the task filter, which is treated as a soft signal rather 
than a hard constraint. This shows that placing strong weight 
on the problem description, without hard filtering on the 
selected task, can lead to recommendations that are clearly 
inconsistent with the user's stated intent.

\begin{comment}

GMM som metode nummer 2 på et supervised classification problem tyder på at 
det å vekte problemstillingen teksten for mye, og ikke ha harde filtere
på task f.eks., kan føre til grove feil. GMM er kanskje der pga "faulty"
i problemstillingen og ordlyden generelt kan minne om en unsupervised anomaly 
detection problem. 

\end{comment}
\section{Ontology Coverage and Structure}

Hva ontologiens coverage og struktur betyr for recommendation.

Hvordan kan den utvides?

Manuelle oppdateringer er tidkrevende (related work).

Dataset type gjennom used\_for.

Tabular + regression = tabular regression.

Mulig sammenligning: Blagec2022 (ITO) bruker automatisk import + manuell 
kuratering. MLguide kunne utvidet på lignende måte med benchmark-data, 
ikke bare attributter.

ML Ontology er egentlig laget for AutoML, ikke for metodevalg, og dette 
setter begrensninger for hva den naturlig kan uttrykke.

\section{Filters and Application Context}

Mangler domenespesifike filtre, belager seg for mye på artikler. 
Akkurat nå kan MLguide bytte artikkeldatabase og være tilpasset et annet 
domene.

Application area og PLC-fase fungerer ikke som filtre, fordi 
klassifiseringen av artikler var for svak (forklart i implementasjon).

Ideelt sett: artikler filtrert på application area, og ontologien skulle 
hatt metode -> used\_for -> domenerettede problemer (predictive 
maintenance, quality control, etc.).

Krever stor manuell gjennomgang per metode, og hvis kunnskapen ikke er 
komplett, kan det være bedre å droppe enn å ha halvferdig.

Konsekvens: domene-orientering er nå kun gjennom artiklene, ikke gjennom 
strukturert kunnskap. Det fungerer delvis fordi artikkelgjenfinning 
fanger topisk relevans, jf. Ostendorff.

Sammenligne med Lickert (reverse logistics) og Sonntag (early product 
development) som er smalere i scope og derfor lettere å bygge dyp 
domenekunnskap for. MLguide dekker hele PLC, mer ambisiøst.

Hvis dette hadde vært på plass, kunne ontologien i prinsippet gjort mer 
av rangeringen alene.

\section{Balance Between Ontology and Literature Retrieval in Recommendations}

\section{Starter Notebooks and Implementation Support}

Notebooks adresserer en av design goals: implementeringsstøtte.

To grupper i P1 nevnte code templates som en fordel, men evidensgrunnlaget 
er begrenset.

Q-Learning manglet template i Example 2. Tegn på at notebook-dekning er 
ufullstendig, samme manuelle-arbeid-problem som ontologien.

Sammenligne med Traingenerator: kodegenerering uten metodevalg. MLguide 
kombinerer de to, men templates er enkle nå. Kunne vært mer metode-spesifikke 
eller mer avanserte (data loading, evaluering osv.).

Hva dette betyr for RQ3: notebooks er konkret støtte til tidlig 
implementering, men formen kan utvikles videre.

Kan utvide til valg av parametere i sidebar, eller forslag til parametere.

Og lett å skalere, trenger bare template i riktig mappe.

\section{Feedback and Use Over Time}

Feedback-mekanismen ble lite brukt (få responser), så den kunne ikke 
evalueres som datakilde (cold start i background).

Avhengig av brukermengde for å være nyttig: før systemet har mange brukere, 
gir tilbakemeldinger lite signal.

Lagrer info som kan brukes til content-based filtering (jf. Recommender 
Systems Handbook). Kombinasjon ontologi + content-based er nevnt i 
litteraturen som lovende, men ikke utforsket her.

Future work: differensiere mellom ekspert og andre tilbakemeldinger, slik 
at ekspertinnspill kan oppdatere kunnskapsbasen direkte. Vanlige brukere 
kan påvirke rangeringen mer indirekte.

\section{Reflection on the Evaluation Approach}

To evalueringsmetoder: user testing og example-based. De utfyller hverandre 
ved at den ene gir uavhengig bruk, den andre gir kontrollert input.

Begrensninger ved user testing:

Tolkning av rapportene var subjektiv siden de ikke fulgte fast struktur.

Få grupper, særlig P2.

Tilbakemeldingsformen ga for lite respons.

Ulike versjoner testet.

Begrensninger ved example-based:

Eksempler konstruert av forfatteren, mulig bias.

Tolkning av output er forfatterens egen, ikke uavhengig.

Ingen sammenligning av embedding-modeller (SciBERT, SPECTER, mpnet osv.). 
Pipeline valgt basert på research, ikke testet mot alternativer i dette 
prosjektet.

\section{Answers to Research Questions}

\section{Limitations and Future Work}

Samle begrensninger som er nevnt gjennom kapittelet.

Og oppsummere potensiell og mest lovende future work-muligheter.

% \subsection{Feedback and Limitations}
% \label{sec:feedback-and-limitations}
% \textbf{Skrives om}

% The feedback from both projects gave useful insight into the usability of 
% MLguide and what could be improved. However, two limitations applied.

% The first concerned the feedback format. Feedback was collected as part of
% the students' project reports, which fit naturally within the course structure
% and allowed students to reflect on MLguide in the context of their own problem
% and data. Since each group worked on a different problem, an open format seemed
% more suitable than a fixed set of questions. The trade-off was that responses
% varied in detail and focus, making it more difficult to draw systematic conclusions.

% The second limitation was that the feedback arrived quite late in the development
% period. This was probably unavoidable, as a working system had to be in place 
% before it could be tested. The feedback led to some adjustments, but larger 
% changes such as new features or structural redesigns were not feasible within 
% the remaining time. These can instead be left as directions for future work.