\section{Changes Across System Versions}
\label{sec:changes-across-system-versions}

The development of MLguide was incremental and iterative, as
described in Section~\ref{sec:incremental-and-iterative-development}.
This form of development
naturally produced different versions of the system at different
points in time, as shown by the development timeline in Section~\ref{sec:development-timeline}. 
This section summarises the central differences
between the three versions that were evaluated. These differences
are relevant to interpret the results and discussion chapters, since
the two user tests and the example-based evaluation were carried
out on different versions of the system.

As the development timeline in
Figure~\ref{fig:development-timeline} shows, the user testing took
place some way into the development period. By the time of the
first user test, all main functionality was in place: the input
form, the recommendation output, the available starter notebooks, 
the display of the top-ranked articles for a given
method, and the feedback button. This functionality was prioritised 
to ensure a working system for the first user test.
The differences between the
three versions therefore lie mainly in the content of the
knowledge graph and in smaller user interface details, rather than
in the core functionality.

Version A, as described above, had the core functionality in place. 
It also included the extension of classes and concepts in the
ontology, described in Section~\ref{sec:ml-ontology-extension}. 
The ML methods used in article retrieval that were missing from the
original ontology were added for version A. At this point, however,
the method properties had not yet been updated. The added methods
therefore carried little beyond an article link. 
The methods already existing
had the properties from the original ontology, summarised by the property coverage in
Table~\ref{tab:ml-approach-coverage}.
Version A had the same user interface as the final
version, which is described in Section~\ref{sec:user-interface}, with only
minor parts not yet implemented.

Version B added the reinforcement learning methods and tasks, with properties, 
since reinforcement learning was entirely absent from the original ontology. 
It also simplified the input form and recommendation process
by removing the filters for task, data type, and performance preferences, leaving
only the problem text input and the ML area filter. 

Version C populated multiple methods with properties, 
adding 116 triples with performance attributes to ML
approaches, and 248 \texttt{used\_for} links between approaches and
tasks, thereby updating the knowledge available to the system. This
resulted in the coverage shown in
Table~\ref{tab:ml-approach-coverage-extended}. Version C also
reintroduced the filters that had been removed from the input form in Version B, and added the
feedback rating option on individual methods, 
as shown in Figure~\ref{fig:user-interface-method-details-page}. 

Table~\ref{tab:system-versions} summarises the main changes in each
of the three versions.

\begin{table}[H]
\centering
\caption[Differences between system versions]{Summary of what was
added or changed in each evaluated version of MLguide.}
\label{tab:system-versions}
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{Version} & \textbf{Added or changed} \\
\midrule
Version A &
$\bullet$~Core functionality in place. \newline
$\bullet$~Ontology extended with new classes and concepts. \newline
$\bullet$~Many methods still without properties beyond an article link. \\
\addlinespace[8pt]
Version B &
$\bullet$~Reinforcement learning knowledge added. \newline
$\bullet$~Input form simplified to include problem description text and ML area filter only. \\
\addlinespace[8pt]
Version C &
$\bullet$~Several performance attributes and \texttt{used\_for} links added to methods, improving the system knowledge. \newline
$\bullet$~Task, data type and performance filters reintroduced in input form. \newline
$\bullet$~Method-level feedback added. \\
\bottomrule
\end{tabularx}
\end{table}


